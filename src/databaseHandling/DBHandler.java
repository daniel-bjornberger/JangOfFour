import java.sql.*;

Class DBHandler{
    Private DBHandler DBHandlerInstance=new DBHandler;
    String searchString;
    int pagenumber;
    int pageAmount;
    int hitsPerpage;
    int hitAmount;

    public getInstance(){
        }


    add(Connection,contact);
    delete(Connection,contact);
    update(Connection,contact);
    searchPage(Connection,contact);
}